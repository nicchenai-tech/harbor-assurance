services:
  harbor-demo:
    build: .
    ports:
      - "8080:8080"
    environment:
      HARBOR_PUBLIC_DEMO: "1"
      HARBOR_OCR_BACKEND: tesseract
      PORT: "8080"
    read_only: true
    tmpfs:
      - /tmp:size=256m
    security_opt:
      - no-new-privileges:true
