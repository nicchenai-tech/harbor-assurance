# No API keys or paid services are required.
# Local defaults are CLI arguments: --data, --runtime, --host and --port.
# Public Docker deployments use these non-secret values:
HARBOR_PUBLIC_DEMO=0
HARBOR_OCR_BACKEND=auto
# HARBOR_DATA=/app/demo-data
# HARBOR_RUNTIME=/tmp/harbor-runtime
